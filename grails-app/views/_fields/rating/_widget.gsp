<g:field
        type="number"
        name="${prefix}${property}"
        required="${required}"
        min="0"
        max="4"
        step="1"
        value="${value}"
        data-size="xs"
        data-stars="4"
        data-show-caption="false"
        class="${pageScope.class} rating"/>